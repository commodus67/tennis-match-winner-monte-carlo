{
  "actorOutputSchemaVersion": 1,
  "title": "ATP/WTA match-winner projections",
  "properties": {
    "matches": {
      "title": "Matches",
      "description": "One row per player per match, with the model probability, live Kalshi price and edge/value verdict.",
      "type": "string",
      "template": "{{links.apiDefaultDatasetUrl}}/items"
    }
  }
}
